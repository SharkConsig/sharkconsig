{
  "entities": {
    "Perfil": {
      "title": "Perfil do Usuário",
      "description": "Dados de perfil e permissões do usuário no sistema SharkConsig",
      "type": "object",
      "properties": {
        "id": { "type": "string", "description": "ID do usuário (UUID do Auth)" },
        "username": { "type": "string", "description": "Nome de usuário único" },
        "email": { "type": "string", "format": "email", "description": "E-mail do usuário" },
        "nome": { "type": "string", "description": "Nome completo do usuário" },
        "role": { 
          "type": "string", 
          "enum": ["Desenvolvedor", "Administrador", "Operacional", "Supervisor", "Corretor", "Estágio"],
          "description": "Função do usuário no sistema"
        },
        "status": { 
          "type": "string", 
          "enum": ["Ativo", "Inativo"],
          "description": "Status da conta"
        },
        "permissoes": { "type": "object", "description": "Configurações de permissões específicas" },
        "created_at": { "type": "string", "format": "date-time" },
        "updated_at": { "type": "string", "format": "date-time" }
      },
      "required": ["id", "username", "email", "role"]
    },
    "Convenio": {
      "title": "Convênio",
      "description": "Lista de convênios disponíveis para propostas",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nome": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" }
      }
    },
    "Banco": {
      "title": "Banco de Empréstimo",
      "description": "Lista de bancos disponíveis para propostas",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nome": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" }
      }
    },
    "TipoOperacao": {
      "title": "Tipo de Operação",
      "description": "Lista de tipos de operação disponíveis para propostas",
      "type": "object",
      "properties": {
        "id": { "type": "string" },
        "nome": { "type": "string" },
        "created_at": { "type": "string", "format": "date-time" }
      }
    }
  }
}
